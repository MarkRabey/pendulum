import Timer from 'renderer/components/Timer';
import Layout from 'renderer/containers/Layout';

const Home = () => {
  return (
    <Layout>
      <Timer />
    </Layout>
  );
};

export default Home;
